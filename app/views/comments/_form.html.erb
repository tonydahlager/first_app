<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :commenters %><br />
		<%= f.text_field :commenters %>
	</p>
	<p>
		<%= f.label :body %><br />
		<%= f.text_area :body %>
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %> 